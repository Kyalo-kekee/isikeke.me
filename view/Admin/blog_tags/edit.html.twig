{% extends 'admin.html.twig' %}

{% block title %}Edit Tags{% endblock %}

{% block body %}

    <div class="col col-md-12">
        <div class="card">
            <div class="card-header">
                <h5> Edit Tag</h5>
            </div>
            <div class="card-body">
                {{ include('Admin/blog_tags/_form.html.twig', {'button_label': 'Update'}) }}

                <a href="{{ path('app_blog_tags_index') }}">back to list</a>

                {{ include('Admin/blog_tags/_delete_form.html.twig') }}
            </div>

        </div>
    </div>

{% endblock %}